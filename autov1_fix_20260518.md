# Auto Refresh Pro 浏览器插件 - 修复记录

## 修复内容汇总

### 1. 窗口尺寸统一 (popup.css)
- body: `height: 520px; overflow: hidden`
- 两个 tab-content 用 `flex: 1; min-height: 0` 均分空间

### 2. 网页弹窗报错 (content.js / background.js / popup.js)
- 未选 XPath 启动 → 右下角红色错误弹窗
- XPath 执行失败 → 右下角红色错误弹窗
- 位置统一 `bottom: 24px; right: 24px`

### 3. 倒计时不跳动 (popup.js)
- 本地 self-tick，不再依赖 background GET_STATUS

### 4. CSP 兼容 (content.js)
- `element.click()` 包 try-catch，兼容 CSP 严格的页面

### 5. XPath 选取失效 (popup.js / background.js)
- popup 走 background 中转，等待确认后再关闭
- 选取结果通过 storage 持久化回传
